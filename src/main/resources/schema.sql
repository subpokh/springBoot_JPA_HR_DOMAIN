
CREATE TABLE customer(
  customerId NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 100 INCREMENT BY 1),
  firstName VARCHAR2(50) NOT NULL,
  lastName VARCHAR2(50) NOT NULL,
  address VARCHAR2(100)NOT NULL,
  phone NUMBER(10),
  hiredDate DATE DEFAULT SYSDATE NOT NULL);
  